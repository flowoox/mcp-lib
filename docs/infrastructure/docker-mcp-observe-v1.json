{
  "schema_version": 1,
  "work_item": "docker-mcp",
  "service": "services/docker-mcp",
  "branch": "feat/docker-mcp-readonly-proxy",
  "base_sha": "b98cfb215664d1fbc77e056df379b20c4b726134",
  "status": "implemented_awaiting_ci",
  "active_pull_request": null,
  "merged_slices": [
    {
      "name": "bounded-logs-and-events-v1.1",
      "pull_request": 37,
      "merge_sha": "95856304c078d223f51201552e919ff7375dd46e",
      "repository_ci_run": 32705974305,
      "repository_ci_run_number": 199,
      "repository_ci_conclusion": "success"
    },
    {
      "name": "bounded-resource-inventory-and-one-shot-stats-v1.2",
      "pull_request": 38,
      "merge_sha": "56effb363fa71ffa337022d8e589a682dc6a3bf0",
      "repository_ci_run": 32706871200,
      "repository_ci_run_number": 202,
      "repository_ci_conclusion": "success"
    },
    {
      "name": "aggregate-first-diagnostic-detail-v1.3",
      "pull_request": 39,
      "merge_sha": "b98cfb215664d1fbc77e056df379b20c4b726134",
      "repository_ci_run": 32708799850,
      "repository_ci_run_number": 205,
      "repository_ci_conclusion": "success"
    }
  ],
  "contract": {
    "family": "flowoox.docker-diagnostics",
    "version": "1.3.0",
    "mode": "observe-read-only"
  },
  "synchronization": {
    "readonly_proxy_branch_created_from_exact_main_sha": true,
    "readonly_proxy_initial_base_sha": "b98cfb215664d1fbc77e056df379b20c4b726134",
    "open_pull_requests_at_start": 0,
    "strategy": "merge only after final CI success, mergeable=true and behind_by=0; preserve unrelated concurrent work"
  },
  "implemented": [
    "explicit read-only Docker Engine backend attestation with fail-closed direct-socket override",
    "fixed GET-only operation allowlist with no arbitrary API path method query or command",
    "bounded daemon health container image volume network log event and one-shot statistics observation",
    "aggregate resource inventory and aggregate-first diagnostic detail under shared query budgets",
    "diagnostic detail derives targets from bounded normalized inventory and skips stats for stopped candidates",
    "typed operation results with actor reason correlation ID and audit envelopes",
    "separate TLS read-only authorization proxy process as the only reference component requiring Docker Unix-socket access",
    "proxy bearer credential loaded from a runtime file and stripped before Docker forwarding",
    "proxy exact API-version method path and query allowlist matching only the docker-mcp GET surface",
    "proxy duplicate and unknown query rejection plus page log event time-window and event-type validation",
    "proxy request-body and ambiguous framing rejection bounded upstream timeout response bytes and concurrency",
    "proxy rejects upstream redirects suppresses raw upstream error bodies and disables request-target access logging",
    "product-neutral proxy environment and hardened systemd deployment examples without committed topology credentials certificates keys or tokens",
    "deterministic policy adversarial and loopback-to-Unix-socket forwarding tests"
  ],
  "security_boundary": [
    "the Docker socket remains protocol-privileged for the proxy process; the MCP service itself should not receive Docker socket access",
    "proxy ingress must use TLS plus bearer authentication and should bind only loopback or a dedicated management address protected by firewall policy",
    "no generic pass-through route exists and incoming bearer credentials are never forwarded to Docker",
    "no exec attach create start stop restart kill delete pull push build generic inspect or arbitrary Engine API operation",
    "logs events and resource statistics never expose live streams and remain independently bounded by MCP and proxy limits",
    "writes remain absent; any future write layer requires a separate identity plus plan approval change verify idempotency pre-state rollback intent and independent readback"
  ],
  "verification": {
    "pr39_final_repository_ci_run": 32708799850,
    "pr39_final_repository_ci_run_number": 205,
    "pr39_final_repository_ci_conclusion": "success",
    "readonly_proxy_repository_ci": "pending",
    "readonly_proxy_merge": "pending"
  },
  "remaining": [
    "optional separately designed Docker write layer only if a concrete approved operational use case requires it"
  ],
  "next_work_package": "fileshare-mcp-readonly-foundation",
  "next_action": "Run full repository CI for the product-neutral Docker read-only proxy slice and merge only if green and zero commits behind current main. Then move to fileshare-mcp read-only foundations: bounded metadata/content sampling, SMB and NTFS ACL evidence, inheritance owner ACE diagnostics, effective-rights evidence where safely determinable, no unbounded file streaming, and the shared query-budget connector model."
}
