{
  "schema_version": 1,
  "work_item": "windows-mcp",
  "service": "services/windows-mcp",
  "branch": "feat/windows-mcp-observe",
  "base_sha": "0170d1c4aac0adffa853aeb5773f2d65313c0e1e",
  "status": "implemented_pending_ci",
  "contract": {
    "family": "flowoox.windows-diagnostics",
    "version": "1.0.0",
    "mode": "observe-read-only"
  },
  "synchronization": {
    "open_prs_at_start": 0,
    "open_issues_at_start": 0,
    "parallel_windows_branches_at_start": 0,
    "strategy": "fresh branch from exact current main after Entra completion; preserve all existing feature branches"
  },
  "implemented": [
    "product-neutral logical Windows target allowlist",
    "fail-closed explicit read-only backend assertion",
    "remote WinRM configuration requiring a constrained non-default JEA endpoint by default",
    "repository-owned static PowerShell probes with no arbitrary command/cmdlet/script surface",
    "host OS boot uptime resource and reboot-pending observation",
    "bounded service inventory without binary paths",
    "bounded process inventory without command line executable path owner or environment",
    "Windows feature inventory",
    "bounded allowlisted event-log diagnostics with opt-in 512-character message previews",
    "fixed LocalMachine certificate-store metadata without key material",
    "bounded installed hotfix inventory",
    "minimal Hyper-V host and VM-state summary",
    "shared query budgets pagination sampling rate limits timeout concurrency and response-byte caps",
    "typed second-stage backend projection rejecting unexpected fields",
    "actor reason and correlation-aware OperationResult and AuditEvent envelopes",
    "portable Linux unit tests plus a real Windows PowerShell host-probe smoke test",
    "dedicated Windows MCP CI workflow and repository Python CI matrix coverage"
  ],
  "security_boundary": [
    "no arbitrary PowerShell shell cmdlet WMI/CIM query registry path event log certificate path or WinRM target",
    "caller selects only deployment-defined logical target IDs",
    "caller input is never interpolated into PowerShell source or argv",
    "child PowerShell uses shell=false bounded input timeout file-backed stdout and byte limits",
    "no service binary path process command line owner executable path environment certificate key credential or raw backend output returned",
    "remote targets use Kerberos and require an explicitly named constrained JEA endpoint unless an operator deliberately disables that guard",
    "writes are absent; any future mutation layer requires a separate identity plus plan approval change verify idempotency pre-state rollback intent and independent readback"
  ],
  "verification": {
    "local_compileall": "passed for services/windows-mcp/src and tests before push",
    "repository_ci": "pending",
    "windows_smoke": "pending"
  },
  "deferred": [
    "security-audit-mcp cross-service findings and policy normalization",
    "file-share ACL and effective-permission diagnostics in a separate fileshare-mcp",
    "deeper Hyper-V VM storage network replication and checkpoint diagnostics in hyperv-mcp",
    "any state-changing Windows operation"
  ],
  "next_work_package": "security-audit-mcp",
  "next_action": "Open PR, run repository and dedicated Windows CI including windows-latest smoke, repair all failures, re-check current main and parallel work, merge only when green and conflict-free, record merge/CI evidence, then begin security-audit-mcp observe aggregation on fresh main."
}
