name: Security Audit MCP

on:
  pull_request:
    paths:
      - "services/security-audit-mcp/**"
      - "packages/mcp-common/**"
      - "constraints/python312.lock"
      - ".github/workflows/security-audit-mcp.yml"
  push:
    branches: [main]
    paths:
      - "services/security-audit-mcp/**"
      - "packages/mcp-common/**"
      - "constraints/python312.lock"
      - ".github/workflows/security-audit-mcp.yml"

permissions:
  contents: read

concurrency:
  group: security-audit-mcp-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: constraints/python312.lock
      - name: Install locked dependency graph
        run: |
          python -m pip install --constraint constraints/python312.lock --upgrade pip
          python -m pip install --constraint constraints/python312.lock ./packages/mcp-common
          python -m pip install --constraint constraints/python312.lock -e "./services/security-audit-mcp[dev]"
          python -m pip check
          python scripts/check_python_lock.py flowoox-mcp-common flowoox-mcp-security-audit pytest pytest-asyncio ruff
      - name: Lint
        run: ruff check packages/mcp-common/src services/security-audit-mcp/src services/security-audit-mcp/tests
      - name: Test
        run: pytest -q services/security-audit-mcp/tests
      - name: Compile
        run: python -m compileall -q packages/mcp-common/src services/security-audit-mcp/src
      - name: Build container
        run: docker build --file services/security-audit-mcp/Dockerfile --tag mcp-security-audit:test .
